[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "yt-pipeline"
version = "0.1.0"
description = "Faceless YouTube render spine (Phase 1)"
requires-python = ">=3.12"

[tool.setuptools.packages.find]
include = ["pipeline*"]
